## Цель работы
Изучить способы реализации алгоритмов с использованием рекурсии.

## Задание 5
Найти значение функции Аккермана \( A(m, n) \), которое определяется для всех неотрицательных целых аргументов \( m \) и \( n \) следующим образом:

\[
A(0, n) = n + 1;
\]

\[
A(m, 0) = A(m - 1, 1) \quad \text{при} \quad m > 0;
\]

\[
A(m, n) = A(m - 1, A(m, n - 1)) \quad \text{при} \quad m > 0 \quad \text{и} \quad n > 0.
\]
